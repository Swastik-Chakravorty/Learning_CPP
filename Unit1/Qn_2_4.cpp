#include<iostream>

using namespace std;

int main()
{
    int a, b;
    cout << "Enter the two digits one by one: " << endl;
    cin >> a >> b;
    cout << "Sum is: " << a + b << endl;
    return 0;
}